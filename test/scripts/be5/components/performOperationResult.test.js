import React          from 'react';
import renderer       from 'react-test-renderer';
//import changeDocument from '../../../../src/scripts/be5/core/changeDocument';
import {HtmlResult, performOperationResult}   from '../../../../src/scripts/be5/components/form';

test('form.performOperationResult() redirect', () => {
//    const component = renderer.create(
//        <Document />
//    );
//
    //const typed = {type: "operationResult", value: {status: "redirect", details: "static/welcome.be"}}
    //const typed = {type: "operationResult", value: {status: "finished", message: null, details: null}}
    //performOperationResult(typed);


//
//    expect(component.toJSON()).toMatchSnapshot();
});

//test('form.performOperationResult() test', () => {
//    const component = renderer.create(
//        <Document />
//    );
//
//    const typed = {type: "", value: }
//    performOperationResult({ content: "Page <b>text<b>." });
//
//    expect(component.toJSON()).toMatchSnapshot();
//});
